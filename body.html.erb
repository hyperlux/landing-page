<!DOCTYPE html>
<html lang="en" class="desktop-view not-mobile-device text-size-larger">
<head>
    <meta charset="utf-8">
    <title>Events - Auroville Community</title>
    <meta name="description" content="Find information about upcoming events, workshops, and activities in our community. Whether you are organizing an event or looking for something to participate in, this is the place to share and discover.">
    <meta name="discourse_theme_id" content="24">
    <meta name="discourse_current_homepage" content="latest">

    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, user-scalable=yes, viewport-fit=cover">
    <link rel="canonical" href="https://aurovillenetwork.org/c/events/13"/>

    <!-- Include your existing stylesheets and scripts here -->

    <style>
        :root {
            --bg-color: #f1ece6;
            --text-color: #333333;
            --card-bg: #ffffff;
            --card-border: #e0e0e0;
            --primary-color: #e17055;
            --secondary-color: #b7410e;
            --sidebar-bg: #2c3e50;
            --sidebar-text: #ecf0f1;
        }

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--bg-color);
            color: var(--text-color);
            display: flex;
            font-size: 14px;
        }

        .sidebar {
            width: 250px;
            background-color: var(--sidebar-bg);
            color: var(--sidebar-text);
            padding: 20px;
            height: 100vh;
            position: fixed;
            left: 0;
            top: 0;
            overflow-y: auto;
            transition: transform 0.3s ease-in-out;
        }

        .sidebar.hidden {
            transform: translateX(-250px);
        }

        .main-content {
            margin-left: 300px;
            padding: 20px;
            width: calc(100% - 250px);
        }

        .hamburger-menu {
            display: none;
            cursor: pointer;
            font-size: 24px;
            position: fixed;
            top: 10px;
            left: 10px;
            z-index: 1000;
            background-color: var(--sidebar-bg);
            color: var(--sidebar-text);
            padding: 5px 10px;
            border-radius: 5px;
        }

        @media (max-width: 768px) {
            .hamburger-menu {
                display: block;
            }

            .sidebar {
                transform: translateX(-250px);
            }

            .sidebar.visible {
                transform: translateX(0);
            }

            .main-content {
                margin-left: 0;
                width: 100%;
            }
        }

        .landing.page,
        .page.events,
        .page.contact {
            margin-bottom: 40px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .title-container {
            text-align: center;
            margin-bottom: 20px;
        }

        .text-container {
            text-align: center;
            margin-bottom: 30px;
        }

        .event-item {
            background-color: var(--card-bg);
            border: 1px solid var(--card-border);
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .event-item h3 {
            margin-